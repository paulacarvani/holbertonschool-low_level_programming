#include <stdlib.h>
#include <time.h>
#include<stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
int numbers = 48;
while (numbers <= 59)
{
putchar (numbers);
numbers++;
}
{
putchar ('\n');
}
return (0);
}
