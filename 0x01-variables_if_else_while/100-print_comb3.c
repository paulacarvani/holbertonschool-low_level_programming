#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
  int a ='0';
  int b = '0';

    while (a <= '9')
      {
       if (a != b)
	 {
	   while (b <= '9')
	   
	   putchar (b);
	   b++;
	   
       
	 putchar (a);
       a++;
	 
    putchar (',');
    putchar (' ');
      }
      }
    
    return (0);
}
